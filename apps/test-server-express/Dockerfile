# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build test-server-express`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t test-server-express`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system test-server-express && \
          adduser --system -G test-server-express test-server-express

COPY dist/apps/test-server-express test-server-express
RUN chown -R test-server-express:test-server-express .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix test-server-express --omit=dev -f install

CMD [ "node", "test-server-express" ]
